export type Geometry = {
    uid: string,
    nome_tipo_peca : string,
    imgUrl?: string,
    status?: string,
    image?: string,
    date?: string
    creation?: string,
    lastModifiedOn?: string,
}